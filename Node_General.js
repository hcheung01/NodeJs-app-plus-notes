// Say hello plus name with Node

function sayHello(name) {
  console.log('Hello ' + name);
}

sayHello('Heindrick');

window is undefined because there is no document objects in Node environment
console.log(window);


NODE MODULE SYSTEM

-what are modules
-why we need them
-how they work
-how to create my own MODULES

The modules built into the core of NODE such as:

os-operating systems
fs-file system
events
http
